# 官方软件（信盈达配套 APP）无法控制小车 —— 问题分析与解决路径

> 整理时间：2026-07-15
> 工程路径：`d:\STM32_Dev_Env\04.SourceCode\Smart_Car_01\Smart_Car - 10`
> 硬件：STM32F103RC 智能车 + 信盈达 WiFi 图传模块（热点 `ZZXYD-xxxx`）+ 双舵机云台
> 当前 git 分支：`feat-beep`

---

## 一、问题现象

- 用**信盈达官方配套 APP**（手机/电脑连上模块热点后）下发控制指令，**既不能控车、也不能控云台舵机**。
- 摄像头画面（`http://192.168.1.1:8080/?action=snapshot`）能正常显示，说明 WiFi 链路、模块供电、视频通道是通的。
- 自写的 **`wifi_ctrl.py`** 用同样的网络链路（TCP 2002）**可以正常控车和控舵机**——证明 STM32 固件、模块透传、舵机驱动本身都是好的。
- 结论：**链路通，但官方 APP 发过来的指令，固件"看不懂"。**

---

## 二、根本原因（核心矛盾）

> **我们刷的是自定义固件，不是信盈达官方固件；而官方 APP 是按信盈达官方协议设计的。**

整个数据通路是这样的：

```
官方 APP  ──WiFi(TCP)──▶  信盈达 WiFi 模块(OpenWrt)
                              │  ser2net 把 TCP 数据【原样】转发
                              ▼
                          USART3 (PB10=TX, PB11=RX, 9600)
                              ▼
                          STM32 自定义固件 wifi_uart.c 解析
```

- WiFi 模块（ser2net）**只做透明串口透传**，不做任何协议翻译。
- 官方 APP 发出的字节，原封不动到达 STM32 的 USART3。
- STM32 跑的是**我们自己写的 `wifi_uart.c`**，它只认"我们约定的帧格式"，而**不认官方 APP 的字节协议**。
- 因此：官方 APP 发的指令，固件要么解析不到、要么解析成非法帧被丢弃 → 表现为"完全控不了"。

**一句话总结：不是模块坏了，也不是 WiFi 不通，而是"官方 APP 的方言"和"我们固件的方言"对不上。**

---

## 三、我们的固件实际认什么（`wifi_uart.c` 解析逻辑）

`WifiUart_Process()` 当前只处理两类数据（`User/API/wifi_uart.c` 第 90~148 行）：

### 格式 A：二进制帧（我们以为的"官方帧"）

```c
if (n >= 4 && (b[0] == 0xAA || b[1] == 0x55))
{
    uint8_t cmd   = b[2];   // 0x00停 0x01前 0x02后 0x03左转 0x04右转
    uint8_t yaw   = b[3];   // → PA8 水平舵机
    uint8_t pitch = (n >= 5) ? b[4] : 128;  // → PA11 垂直舵机
    ...
}
```

- 帧头判定：`b[0]==0xAA` 或 `b[1]==0x55`。
- CMD 只映射 `0x00~0x04` 五个值，其它值一律 `mapped=0` 被忽略。

### 格式 B：单字符兜底

```c
else if (n == 1)   // 网络调试助手直发 '1'~'8'/'2'/'4'
{
    g_wifi_frame_cmd = b[0];
}
```

### 固件解析的脆弱点（这正是官方 APP 对不上的隐患）

1. **帧头判断有逻辑瑕疵**：`b[1]==0x55` 这个分支很可疑。若官方帧是 `AA 00 ...`，则 `b[1]=0x00`，根本不会命中 `b[1]==0x55`；真正能命中官方 `AA` 帧的只有 `b[0]==0xAA`。这个判断是从别处抄来、未经验证的。
2. **CMD 取值范围太窄**：只认 `0x00~0x04`。官方 APP 若用其它编号（如 0x05~0x08、或方向相反的定义），直接被 `default: mapped=0` 丢弃。
3. **依赖"空闲中断"分帧**：帧边界靠总线静默（IDLE）判断。若官方 APP 发送是**连续流式**（帧间间隔极小），两帧会被粘成一包，`n` 变大且 `b[0]` 可能不是帧头 → 整包落入"其它长度/非法帧: 忽略"。
4. **端口假设**：固件不关心端口，但模块 ser2net 只把 **2002** 转发到 USART3。若官方 APP 默认连的是**别的端口**（不是 2002），指令压根不会到 USART3。

---

## 四、为什么自写脚本能控

`wifi_ctrl.py` 之所以能用，是因为它**发的就是固件认的帧**：

- 运动指令发**单字符** `'1'~'8'`（命中格式 B）。
- 舵机指令发 **`AA 00 CMD YAW PITCH`**（命中格式 A 的 `b[0]==0xAA` 分支）。

即：脚本是我们**按自己固件的协议**写的，自然对得上。官方 APP 不是为我们这个固件写的，所以对不上。

---

## 五、官方 APP 为何对不上 —— 几种可能（按可能性排序）

| #   | 可能原因                  | 说明                                                                                   | 验证方法                                                          |
| --- | ------------------------- | -------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 1   | **控制端口不是 2002**     | 官方 APP 可能连模块其它端口（如视频 8080 之外的专用控制端口），ser2net 没转发到 USART3 | 进 OpenWrt 管理页 `192.168.1.1` 看 ser2net 配置；或模块侧 tcpdump |
| 2   | **帧格式/帧头不同**       | 官方帧可能是 `55 AA ...`、`AA 55 ...`、或完全不同的头；或带长度/校验字节               | 抓 USART3 原始字节（见第六节）                                    |
| 3   | **CMD 编码不同**          | 官方前进/后退的字节值与我们映射的 `0x01/0x02` 不一致                                   | 抓包后比对                                                        |
| 4   | **连续流粘包**            | 官方高频发送导致空闲中断分帧失败                                                       | 抓包看每包字节数                                                  |
| 5   | **官方用 ASCII 文本协议** | 如发 `"F"`/`"B"`/`"L"`/`"R"` 而非二进制                                                | 抓包看是否可打印字符                                              |

> PDF 资料（`8.蓝牙&WiFi&舵机.pdf`）里的官方协议都是**代码截图**，文字无法提取，所以官方 APP 的真实字节协议目前**未知**——这是必须靠"抓包"才能确定的事，不能靠猜。

---

## 六、让官方 APP 可用的解决路径

### 步骤 1：抓包，确认官方 APP 到底发了什么

**推荐方案 A：固件回显（最直接，无需进模块）**
在 `wifi_uart.c` 的 `USART3_IRQHandler` 里，把每个收到的字节通过 **USART1**（接电脑串口助手）原样打印出来：

```c
// 在 USART3 收到字节后，顺便从 USART1 回显（仅调试用）
void WifiUart_SendByte_USART1(uint8_t b){ /* 复用已有 USART1 发送 */ }
// 在 RXNE 分支：
//   WifiUart_SendByte_USART1(d);
```

然后打开官方 APP 操作各个按钮，电脑串口助手就能看到**官方 APP 发来的原始字节流**，从而反推出帧格式、端口、CMD 编码。

**方案 B：模块侧抓包**
进 OpenWrt 管理页或用 SSH 登录模块，对 2002（及可能的其它端口）执行 `tcpdump`，直接看官方 APP 连的是哪个端口、发了什么。

### 步骤 2：改 `wifi_uart.c` 适配官方协议

拿到真实帧后，调整 `WifiUart_Process()`：

- 修正帧头判定（按抓到的真实头）。
- 扩展 CMD 映射（按抓到的真实编码）。
- 如需，增加长度/校验处理、或改为"按固定长度分包"而非依赖 IDLE。

### 步骤 3（可选）：改模块 ser2net 端口

若官方 APP 用的是非 2002 端口，在 OpenWrt 里把该端口也映射到 USART3 即可，无需改固件。

---

## 七、当前状态与建议

- **现状**：官方 APP 不可用；只有自写 `wifi_ctrl.py` / 最新版 `wifi_cam.py` 能控（后者待你重新验证）。
- **无需重烧即可验证脚本**：`wifi_cam.py` 的改动只在 PC 侧，固件未动，直接重跑 `wifi-zy.bat` 即可。
- **要让官方 APP 可用，必须先抓包**（步骤 1）。这一步没法绕过——在没有官方协议文本的情况下，任何"猜测式"改固件都是在碰运气。

> 如果你愿意，我可以现在就把"方案 A 固件回显"的代码加进 `wifi_uart.c`（加一个调试开关，默认关闭，不影响现有功能），你烧录后用官方 APP 操作一遍，把串口助手收到的字节贴给我，我来反推协议并改适配。
