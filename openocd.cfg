# OpenOCD 配置：ST-Link v2 + STM32F1 (SWD)
# 若 OpenOCD 版本较新提示找不到 stlink-v2.cfg，把下面第一行改成：
#   source [find interface/stlink.cfg]
source [find interface/stlink-v2.cfg]
transport select hla_swd
source [find target/stm32f1x.cfg]
