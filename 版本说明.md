# 智能车工程 版本说明

> 芯片：STM32F103RC　功能：蓝牙遥控 + 超声波避障 + 红外循迹（引脚预留）+ LCD 状态/图片/动图
> 仓库：`https://github.com/gongchangyi/Smart_Car_01`
> 维护：gongchangyi

---

## 一、版本总览

| 标签 | 含义 | 对应本地文件夹 | Commit |
|------|------|----------------|--------|
| **v1.0** | 初始版本（最早迭代） | `Smart_Car - 10 - 03` | `25267d9` |
| **v2.0** | 第二版（避障改进） | `Smart_Car - 10` | `fd23e3f` |

历史顺序：`v1.0 (-03) → v2.0 (-10)`。

---

## 二、v1.0 初始版本（文件夹 `Smart_Car - 10 - 03`）

**功能清单**
- **蓝牙遥控**：8 条运动指令控制 4 个轮子（前进/后退/左转/右转/原地左右转圈/停止），`'9'`/`'0'` 调速 ±10%。
- **超声波避障**：单点 HC-SR04（车头正前方），`USONIC_IsObstacle()` 判定，**固定阈值 20cm**，遇障统一右转绕行，超时（≈3s）停车。
- **LCD 显示**（ST7735 128×160）：首屏显示「郑州轻工业大学」+ 学号 `542307010327` + 姓名「张毅」+ 电压(占位 7.4V)/速度/状态。
- **图片/动图**：`'O'` 显示静态图，`'A'` 播放动图，`'P'`/`'B'`/`'C'` 返回状态页。
- **红外循迹引脚**：`sensor.h` 已定义 5 路（PC0~PC4），但 `main.c` 中未调用，即循环仅定义了接口、未实现循迹逻辑。

**已知局限（v1.0）**
- 超声波引脚注释**沿用原理图标注 PA4/PA5（实际错误）**，正确应为用户实测 PB14/PC6。
- 避障阈值固定 20cm，较近，高速时反应余量不足。
- 避障转到空旷后**缺少恢复前进的修复**，可能停在停止状态。
- 诊断输出较简单（仅 ECHO 电平 + TRIG 自检）。

---

## 三、v2.0 第二版（文件夹 `Smart_Car - 10`）

在 v1.0 基础上做了**避障相关改进**，其余框架一致。

**相对 v1.0 的改动**
1. **超声波引脚修正**：注释改为用户实测 `TRIG=PB14`、`ECHO=PC6`（均为 FT 5V 容忍引脚，可直连模块 5V 回波），并补充供电说明（VCC 接 5~8V 内部稳压，不会烧）。
2. **避障阈值调整**：`sensor.h` 中 `USONIC_OBSTACLE_CM` 由 20cm 改为 **50cm**（2026-07-11 修改）；同时 `main.c` 实际采用**随速度变化的动态阈值** `Obstacle_Threshold_CM()`：`30 + speed/2`，钳位到 **[60, 85]cm**（低速 60cm、满速 80cm），速度越快越早判障。
   > 注：v2.0 的 50cm 宏仅作记录，实际生效的是动态 60~85cm 逻辑。
3. **避障判定函数化**：新增 `Front_Blocked()`（基于 `USONIC_GetDistance()` + 动态阈值），替代 v1.0 的 `USONIC_IsObstacle()`。
4. **修复恢复前进**：`Avoid_Obstacle()` 返回后补 `Execute_Motion(g_current_cmd)`，确保转到空旷后**真正恢复前进**，而非停在停止态。
5. **诊断增强**：新增 **DWT 计时自检**（`USONIC_DWT_OK()`）与 **1Hz 距离回显**（带错误码 `USONIC_GetLastError()`），方便确认模块是否回波。

**共有功能（与 v1.0 一致）**
- 蓝牙 8 指令 + 调速、4 轮独立驱动、超声波避障、LCD 状态/图片(`O`)/动图(`A`)、红外循迹引脚 PC0~4（仍未在 main 调用）。

---

## 四、两版差异速查表

| 对比项 | v1.0 `- 03` | v2.0 `- 10` |
|--------|-------------|-------------|
| 避障阈值 | 固定 20cm | `sensor.h` 50cm + `main.c` 动态 60~85cm |
| 超声波引脚注释 | 原理图 PA4/PA5（错） | 实测 PB14/PC6（对） |
| 避障判定 | `USONIC_IsObstacle()` | `Front_Blocked()` 动态阈值 |
| 恢复前进 | 缺失（可能停住） | 已修复 |
| 诊断输出 | ECHO/TRIG 基础自检 | + DWT 自检 + 1Hz 距离回显 |
| LCD/蓝牙/动图 | 有 | 有（同） |
| 红外循迹 | 仅定义引脚 | 仅定义引脚（均未调用） |

---

## 五、如何回退 / 切换版本

### 1. 查看历史
```powershell
git log --oneline          # 看到 v1.0 / v2.0 两条
git tag                    # 查看所有版本标签
```

### 2. 只读查看某个旧版本（最安全，不破坏当前代码）
```powershell
git checkout v1.0          # 工作区瞬间变成初始版本，可编译/对比
git switch main            # 看完后回到最新主线
```
> 此操作进入"分离 HEAD"状态，只看不改；改之前务必先 `git switch main`。

### 3. 把"上个版本"作为新起点继续开发（保留历史，推荐）
```powershell
git revert <要撤销的版本commit>   # 生成一个反向提交，历史不丢
```
适合已经推送到 GitHub 的情况。

### 4. 彻底回退到上个版本（会丢掉之后的改动，慎用）
```powershell
git reset --hard v1.0      # 工作区+历史都回到 v1.0，v2.0 之后的提交消失
```
> `--hard` 会**删除未提交及被回退的提交**，本地无法恢复，只用在你确定不要新版本时。

### 5. 推送标签到 GitHub
标签默认不随 `git push` 上传，需单独推：
```powershell
git push origin --tags
```

---

## 六、目录说明（仓库内）

```
Smart_Car - 10/
├── CMSIS/                      # 内核库
├── StartUp/                    # 启动文件
├── STM32F10x_StdPeriph_Driver/# 标准外设库
├── User/                       # 应用代码（main.c / 电机 / 蓝牙 / 超声波 / LCD）
├── tools/                      # 字库/图片生成脚本
├── Makefile / openocd.cfg / STM32F103RC.ld
├── .gitignore                  # 忽略编译产物(Project/ 等)
└── 版本说明.md                 # 本文件
```
